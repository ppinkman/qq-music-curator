# QQ 音乐“我喜欢”规则自动分类工具——实施方案

## 1. 项目结论

项目可行。本版本只读取“我喜欢”歌曲，完成本地规则分类、人工复核和文件导出。

QQ 音乐网页接口并非面向开发者承诺稳定的公开 API，字段、签名、风控和登录态都可能变化。因此，系统必须保证：即使在线接口失效，已抓取的数据、分类结果和导出功能仍然可用，且不会重复抓取或丢失进度。

---

## 2. 产品边界

### 2.1 MVP 必须完成

- 从 `cookie.txt` 或环境变量读取登录凭据，禁止在示例配置中保存真实 Cookie。
- 分页读取全部“我喜欢”歌曲，并按 `songmid` 去重。
- 保存原始歌曲清单和元数据缓存，可断点续跑。
- 按“语种”和“曲风/情绪”两个独立维度进行多标签分类。
- 输出 TXT、CSV 和 JSON；同时生成低置信度/未分类复核清单。
- 支持 `demo`、`fetch`、`classify`、`export`、`run` 五种运行模式。
- 单曲详情失败不影响整体任务，失败项可单独重试。

### 2.2 后续增强

- 获取歌词并用歌词字符分布辅助识别语种。
- 提供人工修正规则，修正结果在以后运行中优先使用。
- 增量同步：只处理新增、删除或元数据变化的歌曲。
- 分类规则版本化，规则变化后可仅重新分类，无需重新联网抓取。

### 2.3 明确不包含

- OpenAI、Codex 或其他 AI 模型分类。
- 自动创建、修改、删除 QQ 音乐歌单，或向歌单写入歌曲。
- 歌词、音频和版权内容下载。

---

## 3. 推荐架构

```text
CLI / main.py
   |
   +-- 配置与凭据层       config.py
   +-- 数据源适配器       qqmusic_api.py
   +-- 本地存储/断点层    repository.py
   +-- 元数据补全层       metadata.py
   +-- 分类与置信度层     classifier.py
   +-- 复核与覆盖规则层   overrides.py
   `-- 导出层             exporter.py
```

### 3.1 数据流

```text
登录态校验
  -> 分页抓取歌曲
  -> 原始数据落盘
  -> 缓存命中 / 补抓详情
  -> 规则分类 + 置信度
  -> 人工覆盖
  -> TXT / CSV / JSON / 报告
```

在线抓取和本地分类必须解耦。用户应能对上一次抓取的数据反复调整规则和导出，而不需要再次提交 Cookie 或访问 QQ 音乐。

---

## 4. 核心设计

### 4.1 配置与账号安全

- `config.json` 只存非敏感设置；真实 Cookie 默认放在 `cookie.txt`，并加入 `.gitignore`。
- 同时支持环境变量 `QQMUSIC_COOKIE`，优先级：环境变量 > `cookie.txt` > 配置文件（仅兼容旧版本）。
- 日志不得输出 Cookie、`skey`、`p_skey`、完整 UIN 或请求头。
- 启动后先调用轻量接口验证登录状态；明确区分“Cookie 失效”“接口变化”“网络超时”和“无歌曲”。
- 默认只读，不自动修改用户账号；远端写操作必须通过显式参数启用。

### 4.2 抓取与容错

- 数据源封装为适配器，避免接口变化扩散到分类和导出模块。
- 分页终止条件同时参考 `total`、返回条数、重复页和空页，避免只抓到第一页或无限循环。
- 请求使用连接复用、有限重试、指数退避、随机抖动和可配置限速。
- 每页抓取后立即写入检查点；程序中断后从检查点恢复。
- 用 `songmid` 作为主键去重，并保留原始响应的必要字段用于排障。
- 详情请求采用有上限的小批量并发；不要为每首歌无限制串行请求。

### 4.3 本地数据模型

建议在 `data/` 中保存：

```text
data/
├── songs.jsonl             # 规范化歌曲清单
├── metadata_cache.jsonl    # 单曲详情/歌词缓存
├── checkpoint.json         # 抓取进度
├── overrides.json          # 人工分类修正
└── failures.jsonl          # 可重试失败项
```

每首歌至少包含：

```json
{
  "mid": "歌曲唯一标识",
  "name": "歌名",
  "singers": ["歌手1", "歌手2"],
  "album": "专辑",
  "language_raw": "接口原始语种",
  "genre_raw": ["接口原始流派"],
  "tags_raw": ["接口原始标签"],
  "categories": {
    "language": ["粤语"],
    "genre": ["摇滚"]
  },
  "confidence": {
    "language": 0.95,
    "genre": 0.80
  },
  "evidence": ["metadata:粤语", "metadata:摇滚"],
  "rule_version": "2.0"
}
```

### 4.4 分类引擎

分类结果必须带有“证据”和“置信度”，不能只返回分类名称。

#### 语种优先级

1. 人工覆盖规则：最高优先级。
2. QQ 音乐明确语种字段：高置信度。
3. 歌词字符、词汇与比例分析：中高置信度。
4. 标题、歌手、专辑启发式：低置信度，仅作为候选。
5. 无可靠证据：归入“未知语种”，不得把中文标题默认判为普通话。

特别注意：粤语和普通话共享汉字，仅凭中文歌名无法可靠区分；日语歌曲也可能使用纯汉字标题。因此低置信度结果必须进入复核清单。

#### 曲风与情绪分离

- 曲风：流行、摇滚、民谣、电子、R&B、嘻哈、爵士、古典、原声等。
- 情绪/场景：抒情、伤感、治愈、安静、热血、运动、学习等。

“流行”不应等同于“抒情”，“R&B”不应和“嘻哈”强制合并。配置应支持：

- `include_any`：命中任一关键词。
- `include_all`：必须同时命中。
- `exclude`：排除词。
- `priority`：规则冲突时的优先级。
- `min_confidence`：进入正式歌单的最低置信度。

### 4.5 导出

默认输出：

```text
output/
├── playlists/              # 各分类 TXT
├── songs.csv               # 全量歌曲、标签、置信度和证据
├── songs.json              # 机器可读完整结果
├── review.csv              # 未分类/低置信度/冲突项
├── failures.csv            # 抓取失败项
└── summary.md              # 分类覆盖率和统计报告
```

- TXT 继续使用 `歌名 - 歌手`，但应先验证当前 QQ 音乐客户端支持的实际导入入口和格式；不要把“粘贴文本即可批量导入”作为未经验证的产品承诺。
- 分类内按原收藏顺序输出并去重。
- 使用临时文件写完后原子替换，避免中断产生半个文件。
- 报告至少显示：总歌曲数、详情成功率、各分类数量、未分类率、低置信度数量、重复数和失败数。

---

## 5. 命令行设计

```powershell
python main.py demo
python main.py fetch
python main.py classify
python main.py export
python main.py run
python main.py retry-failures
```

推荐参数：

```text
--config PATH
--output-dir PATH
--data-dir PATH
--refresh              忽略缓存重新获取
--min-confidence 0.70
```

程序应返回明确退出码，便于脚本化：`0` 成功，`2` 配置错误，`3` 登录失效，`4` 网络/API 错误，`5` 部分成功。

---

## 6. 当前代码审计结论

当前仓库已实现 `main.py`、只读抓取器、规则分类器、导出器、配置、断点缓存和离线测试。主要外部风险是 QQ 音乐网页接口和登录校验随时可能变化；分类和导出部分不依赖联网，可对历史缓存重复执行。

---

## 7. 实施路线图

### Phase 0：修复可运行性

- 修复初始化、分页、超时和错误处理问题。
- 增加 Cookie 脱敏、`.gitignore` 和登录态诊断。
- 为现有代码补充基础单元测试。

**验收**：演示模式通过；使用脱敏固定响应可完整抓取多页并正确去重。

### Phase 1：可靠抓取与缓存

- 规范化数据模型。
- 实现缓存、检查点、失败队列、断点续跑和增量更新。
- 增加详情请求限速与有限并发。

**验收**：中途终止后可续跑；重复运行只请求缺失或过期数据；单曲失败不影响整体。

### Phase 2：可解释分类

- 分离语种、曲风、情绪维度。
- 实现优先级、排除词、置信度、证据和未知分类。
- 加入人工覆盖文件与规则版本。

**验收**：固定测试集结果可重复；所有结果可解释；低置信度项进入复核表。

### Phase 3：完整导出

- 增加 CSV、JSON、Markdown 汇总和复核清单。
- 验证 QQ 音乐当前客户端实际支持的导入方式，并据此调整 TXT 格式和操作说明。

**验收**：分类内无重复；统计数字一致；输出中断不会破坏旧结果。

### Phase 4：稳定性与性能

- 为脱敏接口响应增加契约测试。
- 对详情请求增加保守限速，并评估有限并发。
- 增加规则版本号，规则变化时只重新分类。

**验收**：接口字段变化可被测试发现；大歌单可续跑；重新分类不发起网络请求。

---

## 8. 测试与质量门槛

- 单元测试：Cookie/UIN 解析、分页终止、歌曲解析、规则冲突、置信度、去重、文件名安全。
- 契约测试：使用脱敏后的真实接口响应样本，检测字段结构变化。
- 集成测试：用假 API 跑通抓取、缓存、分类、复核和导出全流程。
- 小流量实测：先限制 20 首，再测试完整歌单。
- 所有测试不得访问真实账号或包含真实 Cookie。

建议 MVP 质量指标：

- 抓取完整率 ≥ 99%（不可用/下架歌曲单独统计）。
- 任务中断后不丢进度。
- 导出重复率 = 0。
- 分类覆盖率和准确率分开统计；宁可进入“未知/待复核”，不要强行误判。

---

## 9. 最终交付顺序

最合理的开发顺序是：

```text
先保证抓全且可续跑
  -> 再保证分类可解释、可修正
  -> 再保证导出稳定、可复核
  -> 最后优化接口兼容性与性能
```

这样即使 QQ 音乐接口以后发生变化，核心资产——歌曲清单、缓存、分类规则和人工修正——仍然保留，修复成本也集中在数据源适配器中。
